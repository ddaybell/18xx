# 2038: Tycoons of the Asteroid Belt — Implementation Roadmap

## Current State Assessment

**What's in good shape (as of PR6/Phase 5):**
- `meta.rb` — solid
- `entities.rb` — all data bugs fixed; minor/corp structure correct
- `map.rb` — full tile set (2001–2023) with correct labels/paths; MINE_DATA and TRANSSHIPMENT_HEXES constants added
- `game.rb` — market, phases, train list, corporation groups, AL formation, TSI Probe (pre-float + retirement), route distance/validation, mine revenue model, base/station/claim infrastructure
- `round/operating.rb` — OR entity ordering, Fast Buck treasury income
- `step/waterfall_auction.rb` — full waterfall auction with minor capitalization and all-pass resolution
- `step/buy_train.rb` — minors allowed to buy trains
- `step/dividend.rb` — full minor (split/retain) and corporation (payout/half/withhold) dividend with stock price movement
- `step/route.rb` — choose-per-hex route tracing with mid-route exploration, MP enforcement, greedy pickups, ship selection, refueling, IF/DH/LY second-draw exploration power, Torch's +1 MP
- `step/buy_infrastructure.rb` — bases, refueling stations, claims (Phase 5)
- `step/special_choose.rb` — Asteroid League formation trigger
- Independent special abilities (Phase 7) — FB/IF/DH/OC/TH/LY all implemented; browser verification of the IF/DH/LY redraw flow still pending
- Corporation home-base delivery bonuses (Phase 7b) — VP/LE/MM/OPC/RCC's `delivery_bonus:`/`delivery_bonus_amount:` in entities.rb, wired into `Game#trace_revenue` via `home_delivery_bonus`
- Growth Corp formation (Phase 8) — `step/buy_sell_par_shares.rb`'s `G2038::Step::BuySellParShares` (exchange buttons in the par UI) + `Game#form_growth_corporation!`, treasury-priced shares, and 1822-style per-ship pilot assignment for the inherited special ability
- Asteroid League formation and independent mergers (Phase 9) — `Game#form_asteroid_league!`, reserved AL shares, `step/merge_into_league.rb`'s `G2038::Step::MergeIntoLeague` (voluntary + recurring), Phase 5/bankruptcy mandatory triggers, AL ship limits, AL reserved base/claim capacity
- Private company post-auction abilities (Phase 10) — TS/VA/RS's one-time free base/station/claim, `step/buy_company.rb`'s `G2038::Step::BuyCompany`
- Endgame & scoring (Phase 11) — bank-depletion end timing (default engine behavior, no override needed), corporation-only must-buy-a-ship-or-bankrupt (`Game#must_buy_train?`), final scoring (`Game#player_value`, Phase 5+ private-company exclusion; AL shares and the Spreadsheet tab already worked with no changes)
- `assets/app/view/game/part/track.rb` — HIDE_TILE_TRACK support; blue hex paths invisible when not on active route

**What needs work next:**
- Live in-browser playtesting — confirmed working by the user (2026-08-02): Phase 7 independent abilities, Phase 9 AL formation/mergers, Phase 10 private company abilities, and Phase 8's per-ship pilot assignment. Still outstanding: Phase 9's AL-share-purchase mechanic (buying stock in an AL share once it's reserved/released) hasn't specifically been exercised yet; Phase 11 (endgame/scoring) hasn't been reached live at all — no game has yet run to conclusion without hitting a bug first.
- Cosmetic/visual polish (Phase 12) — starfield art for unexplored blue hexes, asteroid art for mine tiles, base-structure art for bases, and per-ship-type art for the flight marker (currently a generic icon that only varies by remaining MP). None started; requested by the user, no rules/state impact.

---

## PR Index

| PR | GitHub # | Status | Scope |
|---|---|---|---|
| PR0 | — | merged | Initial game scaffold (meta, market, phases, trains, map/entity stubs) |
| PR1 | #12703 | merged | Phase 1: entity data fixes + map data fixes |
| PR2 | #12806 | open | Phase 2 auction + Phase 3a–3b OR foundation |
| PR3 | — | pending PR2 | Phase 3c–3e dividend + route steps |
| PR4 | — | in progress | Phase 4 routing mechanics + mine revenue model |
| PR5 | — | planned | Phase 5 infrastructure (claims/bases/refueling) + Phase 4c–4j routing completion |
| PR6 | — | planned | Phase 6 TSI probe + Phase 4i undo logging |
| PR7 | — | implemented, needs browser verification | Phase 7 independent special abilities |
| PR7b | — | implemented, needs browser verification | Phase 7b corporation home-base delivery bonuses |
| PR8 | — | implemented, needs browser verification | Phase 8 Growth Corp formation |
| PR9 | — | implemented, needs browser verification | Phase 9 Asteroid League formation + independent mergers |
| PR10 | — | implemented, needs browser verification | Phase 10 private company post-auction abilities |
| PR11 | — | implemented, needs browser verification | Phase 11 endgame & scoring |

---

## Recommended File Structure (New Steps Needed)

```
g_2038/
  step/
    waterfall_auction.rb           ← exists ✓ (PR2)
    buy_train.rb                   ← exists ✓ (PR2)
    dividend.rb                    ← exists ✓ (PR3)
    route.rb                       ← exists ✓ (PR3; tile bag pending PR5)
    buy_infrastructure.rb          ← bases, refueling stations, claims (PR5)
    buy_sell_par_shares.rb         ← exists ✓, growth-exchange choices added (PR8)
    merge_into_league.rb           ← exists ✓, independents join AL (PR9)
```
(No separate `form_growth_corporation.rb`/`form_asteroid_league.rb` files were ever needed — both live entirely as `Game#form_growth_corporation!`/`Game#form_asteroid_league!` in `game.rb`, triggered from existing steps rather than dedicated ones.)

---

## Phase 1: Entity & Data Fixes (Prerequisite for everything else)

**Goal:** Get the data layer correct before building mechanics on top of it.

### 1a. Fix `entities.rb`
- [x] Fix Torch minor: `sym: 'TT'` → `sym: 'TH'` *(PR1)*
- [x] Fix Ice Finder minor: `coordinates: 'G7'` → `coordinates: 'M13'` *(PR1)*
- [x] Fix VP corporation: `coordinates: 'J1'` → `coordinates: 'J2'` (J1 is not on the map) *(PR1)*
- [x] Fix all corporation `type:` values from strings to symbols (were `'group_a'` etc.; `game.rb` compares with `:group_a` — would silently break group partitioning) *(PR1)*
- [x] Fix AL `type: 'groupD'` → `type: :group_d` (inconsistent casing and wrong type) *(PR1)*
- [x] Fix `color: :'#xxxxxx'` (Ruby symbol) → `color: '#xxxxxx'` (string) for VP, OPC, RCC, AL *(PR1)*
- [x] Remove placeholder `tile_lay` abilities from TS, VA, RS; replaced with TODO comments pointing to Phase 10 *(PR1)*
- [x] Fix AE's `when: ['Phase 3', 'Phase 4']` → `when: %w[3 4]` to match framework phase name strings *(PR1)*
- [x] Fix same `when:` guard on all independent company exchange abilities (FB, IF, DH, OC, TH, LY) *(PR1)*
- [x] Verify that the dual COMPANIES/MINORS structure (same sym appears in both) works correctly in the framework — COMPANIES is the auctioned certificate, MINORS is the operating entity. Exercised repeatedly via script across this session's auction/dividend/merger work (`buy_company` floating the matching minor, `merge_independent_into_al!`/`form_growth_corporation!` closing both), never an issue. *(PR5?, confirmed via later testing)*

### 1b. Fix `map.rb`
- [x] Fix LOCATION_NAMES typo: `'J18' => 'OCP'` → `'J18' => 'OPC'` *(PR1)*
- [x] Ice Finder LOCATION_NAMES entry was already correct at M13 — the bug was only in the MINORS coordinates (fixed in 1a) *(PR1)*
- [x] Verify all remaining LOCATION_NAMES coordinates are correct *(PR1)*
- [x] Revise asteroid tile definitions (2001–2022) to correctly model N/I/R labels and unclaimed/claimed revenue values; displayed city revenue is the real **unclaimed** value (claimed values live in `MINE_DATA` and are applied by pickup logic) *(PR1; values PR4)*
- [x] Verify double-mine tile (2009) `city=;city=` structure is correct *(PR1)*
- [x] Define a "base tile" (gray side of explored hex: base marker + refueling slot, no track) — tile 2023 *(PR1)*
- [x] Confirm all blue unexplored hexes are listed and have blank content — all 100 blue hexes in `HEXES` *(PR1)*
- [x] **Revised: all tiles carry no functional track (PR6/Phase 5 follow-up).** Hex-to-hex adjacency is purely geometric (`Game::Base#connect_hexes`, grid coordinates only) — G2038's routing never inspects a tile's paths, so the original `BX6`/`SP6`/`DP6` 6-way junction templates were pure decoration with no functional purpose. Removed entirely: single-mine tiles, the base tile (2023), starting-base hexes, transshipment points, and unexplored blue hexes are now plain `city=revenue:X` (or blank) with zero paths — a lone city centers itself automatically (`Tile#compute_city_town_edges`). The one exception is double-mine tiles: their two cities need explicit `loc:0`/`loc:3` plus a cosmetic-only link between them (`MINE_LINK` in `map.rb`, `track:thin`) so the city positions actually respect the tile's rotation — without any path at all, the engine's default path-less-multi-city placement ignores rotation entirely, which would make `explore_hex!`'s random-rotation-for-visual-variety a no-op. Verified: game construction, city centering, `MINE_LINK`'s `:thin` track type, and rotation-driven placement variety across multiple explored double-mine tiles all confirmed via direct testing.
  - **Bug found and fixed immediately after**: `Game::Base::IMPASSABLE_HEX_COLORS` defaults to `%i[blue gray red]` — colors treated as impassable *unless* the hex's own tile has a path pointing back at the neighbor (`Hex#targeting?`, checked in `connect_hexes`). With zero paths anywhere, every blue/gray hex (i.e. almost the entire map) silently lost all its neighbor connections — `hex.neighbors` came back empty everywhere, breaking all ship movement, not just one entity. Fixed with a one-line G2038-only override: `IMPASSABLE_HEX_COLORS = [].freeze` (no shared-code change; this is exactly the per-game override hook the constant exists for). There's no genuinely impassable terrain in this game. Verified: full map connectivity restored (spot-checked 20 random hexes, all connected), `hexes_in_range` back to normal cluster sizes, and the Probe can move to all 6 of TSI's home-hex neighbors again.
- [x] Add mechanism to track which hexes have been explored — `@mine_state` hash on game object *(PR4)*
- [x] Add `MINE_DATA` constant mapping each tile number (2001–2022) to per-mine ore type and unclaimed/claimed revenue values *(PR4)*
- [x] Add `TRANSSHIPMENT_HEXES` constant listing the five gray delivery hex IDs (A13, D2, H10, O11, H18) *(PR4)*

### 1c. Fix spaceship train data model
- [x] **Naming convention:** Spaceships are now named `movement/cargo_holds` (e.g. `'3/2'`, `'5/1'`) matching the physical spaceship cards. `distance:` holds movement points; `cargo_holds:` is the custom field for max mine pickups. All phase `on:` triggers, `rusts_on:` arrays, and `discount:` keys updated to match. *(PR1)*
- [x] **Added `cargo_holds:` field** to every entry in TRAINS (top-level and variants). The base engine stores unknown keys safely in `Train#@opts`. `cargo_holds_for_train` parses the value from the train name (second component) rather than `@opts` for simplicity. *(PR1)*
- [x] **Added `rusts_on:` to variant sub-hashes** — they were previously missing from variants, meaning the engine would use the primary train's rusts_on for all variants of a group. *(PR1)*
- [x] **Probe documented:** 4 movement, 0 cargo holds. Belongs to TSI; not bought from bank. Retirement on TSI's first real spaceship purchase is handled by the ST private's close ability. *(PR0)*
- [x] Verify train obsolescence (`rusts_on:`) matches the rules chart — confirmed correct. *(verified PR4)*

---

## Phase 2: Auction Round

**Goal:** Complete the initial auction so the game can start.

- [x] **2a. Minor capitalization rounding** — rule is `$100 + ½ of (price - $100)`, rounded down. Code: `capital = (price - 100) / 2; bank.spend(100 + capital, minor)` — correct Ruby integer division. *(PR2)*
- [x] **2b. TSI share distribution** — when ST is bought, `after_buy_company` in game.rb gives the buyer the TSI president cert and pars TSI at $100 (full) or $67 (short game). MARKET has `100p` and `67p` at the correct positions. *(PR2)*
- [x] **2c. AE certificate timing** — formation is a **player-triggered choice** (§8), not an automatic grant: AE's `choose_ability` (`G2038::Step::SpecialChoose`, added to both `stock_round` and `operating_round`) lets its owner declare formation from Phase 3 on; `Game#form_asteroid_league!` pars AL at $125, grants the president's cert, adds the $250 initial capital, and force-floats AL directly (it's active immediately, unlike a Public Corp needing 50%). If not declared by Phase 4, `event_asteroid_league_must_form!` (fired by the 6/5 train, replacing an incorrect leftover `close_companies` event that would have force-closed PI/TS/VA/RS too) forces it using AE's current owner. Does **not** include independent mergers into AL (voluntary/mandatory) or AL ship limits/reserved bases-claims — those remain Phase 9 (9c-9h). *(PR6)*
- [x] **2d. All-pass resolution** — `all_passed!` sets `@process_round_end_auction = true`; `resolve_bids` processes all pending bids in order; `round_end_auction_complete` fires `payout_companies` + `or_set_finished` and unpauses all entities. *(PR2)*
- [x] **2f. All-pass edge cases when the cheapest company has no bids** — confirmed with the user and fixed, then corrected again after the user clarified the actual intended flow (see 2g). Case 1: bid-upon-but-unsold companies still resolve in value order even when `@companies.first` itself has no bids (`resolve_bids`'s per-company loop already skipped zero-bid companies without breaking, so this needed no change). Case 2: a leftover, unsold ST leaves TSI correctly unfloated; `probe_bonus_recipient`/`acting_for_entity`/`operating_order` were all tightened to check `owner&.player?` rather than bare truthiness (a leftover ST stays `owner == nil`, not bank-owned -- see 2g) -- without that, the Probe bonus could leak to the wrong recipient, and TSI could be inserted into the operating order with no one to act for it (nil `acting_for_entity`, a crash in `Round::Operating#start_operating`'s log line). *(post-Phase-11 fix; superseded in part by 2g)*
- [x] **2g. There is no separate pre-game auction round at all** — corrected twice by the user before landing on the actual design. First correction: leftovers don't hand off to ordinary Stock-round play (they get re-auctioned). Second, more fundamental correction: the "first SR" *is* a normal Stock round, not a distinct Auction round that later hands off to one -- it just happens to also carry the waterfall-bidding step. As long as no one triggers an all-pass, the round stays open exactly like any SR: once the last company sells (via direct buy or a resolved bid), a turn simply flows straight into ordinary share-buying with no round transition at all. The round only ends the normal Round::Stock way (every entity passed), whether or not anything was ever left unsold.
  - `Game#init_round` now returns `new_stock_round` directly (no `Engine::Round::Auction`/`new_auction_round` at all) -- had to build the log line manually (`round_description('Stock', 1)`) rather than calling `new_stock_round`'s own wrapper, since that reads `@round.round_num` as a fallback and `@round` is still nil this early in `Game::Base#initialize`.
  - `Game#stock_round` now includes `Engine::Step::CompanyPendingPar`/`G2038::Step::WaterfallAuction` ahead of `BuySellParShares` -- WaterfallAuction blocks a turn (bid/pass on companies) for as long as any remain, and its own `actions` goes empty the instant `@companies` is, at which point that same turn (or the next) flows straight into ordinary share buying with no extra step to pass through.
  - `next_round!` is back to the plain, uniform 3-case Stock/Operating structure with no Auction-round special-casing at all -- since `init_round` is now a real `Engine::Round::Stock`, the existing `when Engine::Round::Stock` branch already handles the very first round's ending uniformly with every later one.
  - `Game#initial_auction_companies` stays overridden (`owner.nil? && !closed?`) -- still needed since a fresh `WaterfallAuction` step instance is built for every new Stock round and must only re-list what's actually still unsold.
  - Discovered mid-fix: `Round::Stock#start_entity` re-runs **every** step's `setup` at the start of each entity's turn, not just once per round. The base `WaterfallAuction#setup` (`setup_auction` → `@bids = Hash.new{...}`) would silently wipe out bids placed by earlier players in the same round every time the next player's turn began. Fixed with a guard (`return if @bids`) so only the first call for a fresh step instance actually initializes anything.
  - `WaterfallAuction#round_end_auction_complete` is now nearly empty (`@process_round_end_auction = false`) -- no clearing `@companies` (leftovers just stay in place, still unowned, to be re-offered on a later turn or round), no unpassing entities (needed so `Round::Stock#finished?`'s own all-entities-passed check can actually fire), and no `payout_companies`/`or_set_finished` call (that would double-pay every owned company's revenue on top of the normal payout `Round::Operating#setup` already does at the start of the OR set that follows).
  - Second bug found via testing, not by inspection: when an all-pass resolves into a still-pending mini-auction (a bid-upon company had 2+ bids), `Round::Stock#finished?` would see every entity's `.passed?` flag still true (nothing had unpassed them yet) and consider the round over *immediately*, ending it out from under the pending resolution before the mini-auction's bidders ever got to act. Fixed in `resolve_bids`: only when `all_bids_processed?` is genuinely true does `round_end_auction_complete` fire; otherwise `entities.each(&:unpass!)` keeps the round open until the mini-auction (and anything still queued behind it) actually resolves down.
  - Verified via script against the real action-processing pipeline (`game.process_action`, not the individual methods in isolation): bids placed on separate players' turns survive to the next turn instead of being wiped; a company bought directly (no bidding) doesn't end the round -- the same round continues into ordinary share buying; an all-pass with one single-bid and one multi-bid company correctly keeps the round open through the mini-auction's own resolution and only *then* transitions to the OR set; a later Stock round (after that OR set) correctly re-offers exactly what's still unsold. *(post-Phase-11 fix, supersedes both the original 2d and the first pass at 2g)*
- [x] **2e. Auction ordering** — confirmed via manual playtesting that companies sell in order "0" through "11"; no automated test added. *(verified manually)*

---

## Phase 3: Operating Round Structure

**Goal:** Implement the OR sequence and independent/corporation turn flow.

- [x] **3a. OR entity ordering** — `bank_sort` in `game.rb` uses `ENTITY_DISPLAY_ORDER` to sort independents (FB→IF→DH→OC→TH→LY) before corporations. Corporations operate in descending stock price order via the engine's default OR ordering. *(PR2)*
- [x] **3b. Private company income** — `payout_companies` (called in base `Round::Operating#setup`) handles all private revenues. `G2038::Round::Operating#setup` calls `super` then adds Fast Buck's $15 directly to the FB minor's treasury. *(PR2)*
- [x] **3c/3d. Turn sequence** — `operating_round(round_num)` (the base engine's designated override point -- NOT `new_operating_round`, which handles the "-- Operating Round N --" log banner and `@round_counter` increment before delegating here; overriding `new_operating_round` directly, as an earlier version of this file did, silently drops both) uses `G2038::Round::Operating` with step stack: `Bankrupt`, `DiscardTrain`, `G2038::Step::Route`, `G2038::Step::Dividend`, `G2038::Step::BuyTrain`, `BuyCompany`. Route and Dividend steps are fully implemented; base/station/claim placement steps are Phase 5. *(PR3; fixed PR4)*
- [x] **3e. OR cleanup hook** — `or_round_finished` resets all `mine[:used]` flags to `false` at end of each OR. *(PR3)*

---

## Phase 4: Spaceship Routing (Biggest Engineering Challenge)

**Goal:** Implement the custom route runner for XdYc spaceships.

- [x] **4a. Hex-based movement model** — trace-based: one MP per hex entered; `choices` in the route step offers only adjacent hexes while MP remain; `route_distance` = `route.hexes.size - 1`; `route_distance_str` shows "3H" etc. *(PR4)*
- [x] **4b. Dual constraint enforcement** — MP limit enforced per click (illegal moves are simply not offered, and `process_choose` raises on hand-crafted invalid choices); pickup choices are only offered while a cargo hold is free. *(PR4)*
- [x] **4c. Route origin/destination rules**: *(PR4)*
  - [x] Route must **start** at one of the company's own bases — `start_choices` offers only the entity's placed token hexes *(PR4)*
  - [x] Route must **end** at any base OR a transshipment point to deliver — `trace_revenue` gates on `deliverable_destination?(trace.last)`; undelivered loads are lost (logged); exploring-only routes earn $0 *(PR4)*
  - [x] Ending at a transshipment point with an **empty** cargo hold earns its printed (transshipment) value; visiting one mid-route pays nothing *(PR4)*
  - [x] **Transshipment pickup is a player choice, not automatic** — confirmed with the user: the rules permit a ship to end its flight at a transshipment point *without* collecting there, so `pick_up_transshipment!` is no longer called from `launch_at`/`move_to` on arrival. Instead `transshipment_choice` (mirrors `pickup_choices`) offers a `TRANSSHIP` choice whenever the current hex is a transshipment point with a free cargo hold, aliased onto a click on the current hex itself exactly like a mine pickup (`alias_current_hex_pickup!` now treats `PICKUP`/`TRANSSHIP` as one combined "exactly one -> alias it" pool). Confirmed with the user that choosing to collect also immediately ends the flight (`process_choose`'s `TRANSSHIP` branch calls `pick_up_transshipment!` then `finish_route` together, unlike an ore pickup which lets the ship keep flying) — a side effect of this is that a transshipment credit can now only ever be picked up at the route's actual ending hex, never mid-route, which already matches the pre-existing revenue rule above. `maybe_auto_finish!` (MP-exhausted auto-completion) now also holds off if a transshipment choice is still pending, same as it already did for ore pickups. Verified via script against the real step (`compute_choices`/`process_choose`), not just the isolated method. *(post-Phase-4 fix)*
- [x] **4d. Refueling station bonus** — when a ship belonging to the owning corporation enters a refueling station hex, remaining MP is bumped by up to 3 (capped at the ship's max) — `Step::Route#refuel!`, called from `move_to` when `Game#refueling_station_owner(hex.id) == entity` and not already refueled at that hex this flight; `Game#hexes_in_range` applies the same top-up when computing reachability. *(PR5, checkbox was stale)*
- [x] **4e. Used Mine enforcement** — the Used Mine marker is placed at pickup time (`mark_mine_used!`); cancelling a route returns its loads and un-marks their mines; `or_round_finished` resets the flags each OR *(PR4)*
- [x] **4f. Claimed mine access** — pickup choices skip mines claimed by others; `pickup_value` pays claimed value to the owner *(PR4; claim placement is Phase 5)*
- [x] **4g. Route valuation and pickup selection** — pickup is a **per-hex-entry choice** (matching the rules): while on a mine hex, the player may pick up a load per available mine, hold space permitting. Loads cannot be jettisoned, so an early pickup can block a better mine found later in the run. Cargo aboard is shown in the step help text. *(PR4)*
- [x] **4h. Exploration mid-route** — implemented via the **choose-per-hex model** (supersedes the earlier blended client/server design): *(PR4)*
  - [x] Route tracing uses the standard `choose` action pattern — each hex click fires `Action::Choose` with the hex id (`assets/app/view/game/hex.rb` choose hook); the trace accumulates in `G2038::Step::Route` state and replays deterministically. No engine changes; no node/path-walking (which explodes on 2038's junction hexes). *(PR4)*
  - [x] Only legal next hexes (adjacent to trace tail, MP remaining) are clickable — `available_hex` = `choices` membership; choice buttons mirror map clicks; traced hexes highlight via `@round.laid_hexes` *(PR4)*
  - [x] Entering an unexplored blue hex reveals its pre-assigned tile **immediately mid-route** — the player sees the mines before choosing the next hex *(PR4)*
  - [x] Owning company receives $10 exploration bonus to treasury (not route revenue) *(PR4)*
  - [x] Previously explored hexes are traversed at normal MP cost *(PR4)*
  - [x] Per-hex undo comes free — each hex click is one engine action *(PR4)*
  - [x] Route completion (`finish` choice) computes revenue via `trace_revenue`/`trace_pickups` (greedy, cargo-hold-capped, delivery-gated), builds a passive `Engine::Route` (seeded `hexes:`/`revenue:`; never path-walks) for the dividend step, and logs the run *(PR4)*
  - [x] **Exploration commits the route:** once a trace has explored a hex, the Cancel choice disappears and the Pass button is withheld — the route can only be finished (hidden information was revealed; there is no taking the flight back). Routes with no exploration remain freely cancellable and re-doable. *(PR4)*
  - [x] **"Fly over without exploring" choice** — entering an unexplored hex now offers two options whenever affordable: `hex.id` ("Explore", 2 MP: 1 fly + 1 explore) and `flyover_<hex.id>` (1 MP, tile stays hidden, no mine access, no bonus). Since flyover never costs more than explore, if `hex.id` is offered `flyover_<hex.id>` always is too. *(PR4)*
  - [x] **Hex-anchored popup for the explore/flyover choice** — clicking the hex directly on the map (rather than the bottom choice panel) shows a small "Explore"/"Fly over"/✖ popup positioned at the hex, mirroring `tile_confirmation.rb`'s look exactly. Built as a **generic, opt-in extension** (`G2038::Step::Route#hex_choice_popup(entity, hex)`, `Lib::HexChoicePopup`, `View::Game::HexChoicePopup`) — every other game's `step.respond_to?(:hex_choice_popup)` is false, so behavior is unchanged for them. **This piece touches two shared frontend files (`hex.rb`, `map.rb`) and should be submitted as its own PR** so reviewers can evaluate the core-file tweak in isolation from the rest of PR4/5. *(PR4, split out for review)*
  - [ ] Server-game ergonomics: one HTTP POST per hex click (fine for hotseat; revisit before live play) *(PR5+)*
- [x] **4i. Exploration undo policy** — see Architectural Decision B. In-step cancel is already blocked after exploration (4h); the engine-level **Undo** button cannot be blocked (it rebuilds the game from `@raw_actions` with no game-specific hook to intercept it), so instead: whenever an undo reaches back far enough to un-happen an exploration, log a permanent warning naming the player and hex — confirmed with the user this should announce the peek without disclosing what was actually found, so the fix doesn't broaden the hidden-information leak beyond what the peek itself already caused.
  - **Detection**: reuses `Game::Base.filtered_actions` itself (the exact logic that already decides what an undo removes) rather than guessing from the raw action's shape, as originally sketched — run it once on `@raw_actions` and once with the incoming undo appended; whichever action ids flip from kept to undone are what this undo actually removes. Cross-referencing those ids against `@log` (`GameLog::Entry#action_id` already tags every line with the action that produced it — the user's own suggestion, and the key simplification over the original plan) finds any `"<entity> explores <hex>:"` line among them, with no need for a second full game replay.
  - **Persistence**: the check has to run using the *pre-undo* instance's still-intact `@log`, since an `Action::Undo` short-circuits `Game::Base#process_action` into `clone(@raw_actions)` — a brand new `Game` instance replayed from scratch — rather than mutating the current one. The resulting warning is appended as a genuine follow-up `Action::Message` on the *returned* (post-undo) instance, since `Action::Message` is the one action type `filtered_actions` guarantees can never itself be undone (`when 'message'`) — so it's not just a live-moment notice, it's baked into the action history and reappears identically on every future reload of the game. Verified via script: reloading a game fresh from its raw actions reproduces the exact same warning.
  - **Two real bugs found only by testing this end-to-end, not by inspection**: (1) `entity_type` mismatch — `Step::Message#actions` requires a player entity, but `action.entity` for an in-OR undo is often a corporation; fixed by resolving through `Game::Base#acting_for_entity` (the corp's president) the same way the rest of the game already does. (2) `Action::Base#to_h` memoizes its result the first time it's ever called on a given instance (`@_h ||= {...}`) — the detection logic's own `.to_h` call on the incoming undo action, made *before* `Game::Base#process_action` assigns its `id`, permanently cached a hash with the `id` key missing entirely, which then corrupted the real clone's own use of that same `.to_h` call and broke replay for the rest of the game's life ("comparison of Integer with nil failed" in `process_to_action`). Fixed by assigning the id myself (the same value `super` would assign anyway) before the first `.to_h` call happens.
  - Also fixed in passing: the existing `shipify_log` post-processing in this same `process_action` override assumed every new log entry's `.message` was a String and called `.gsub` on it unconditionally — true for ordinary game-log lines, but a live chat message logs the raw `Action::Message` object instead (per `Step::Message#process_message`), which would have crashed the same way the new warning initially did. Guarded with `entry.message.is_a?(String)`. *(post-Phase-11 addition, supersedes the original initialize_actions-override sketch)*
- [x] **4j. Multiple ships per company** — the player explicitly **picks which unrun ship flies**, via a dedicated tab-style UI (`View::Game::ShipSelector`) that visually mirrors the standard train-selector's bordered/colored tabs from other games (`route_selector.rb`), keyed by the ship's unique `train.id` so two ships of the same type resolve unambiguously. Built as a **generic opt-in component** — any step implementing `ship_choices(entity)`/`current_ship_choice(entity)` gets the same tab UI for free; `G2038::Step::Route` is the only current user. The pick can be switched any time before launching a base hex; entities with only one ship skip the picker entirely (`ship_choices` returns `{}`), matching the "no extra click when there's no real choice" rule used elsewhere in this step. Pass skips remaining ships. Note: unlike `RouteSelector`, this does **not** support building two ships' routes in parallel — one trace in progress at a time; picking a different ship mid-flight isn't offered (`ship_choices`/panel choices are mutually exclusive by design, see `choices` in `step/route.rb`). *(PR4)*
- [x] **Additional: Used Mine marker** — a red X renders on a mine's city on the map as soon as it's marked used (`Part::City#mine_used?` reads `@game.mine_used?(hex_id, mine_idx)`, gated so every other game no-ops); clears automatically each OR via the existing `or_round_finished` reset. *(PR4)*
- [x] **Additional: `HIDE_TILE_TRACK = true`** — paths invisible when not part of an active route; implemented in `assets/app/view/game/part/track.rb` via Snabberb store access to `@game`. Since going trackless (see 1b above), this now only has anything to hide on double-mine tiles (their `MINE_LINK` connector) — everything else has zero paths, so the hook is simply a no-op for them. Still needed: the connector is never part of a real highlighted route, so it would otherwise always render. *(PR4; scope narrowed PR6)*
- [x] ~~**Additional: `skip_route_track_type(:broad)`**~~ — removed in PR4; nothing path-walks under the choose-per-hex model, so the guard became dead code *(PR4)*
- [x] **Additional: `can_run_route?`** — overrides graph connectivity check (2038 has no rail network); returns `route_trains(entity).any?` *(PR4)*
- [x] **Additional: cancel an already-completed route** — undoes a just-finished (not just in-progress) route, as long as it explored nothing and neither did anything run after it this turn (walking backward from the last ship flown; the first explored route found blocks it and everything at-or-before it, since exploration is the one truly irreversible action and its $10 bonus is already paid — unlike plain revenue, never disbursed until Dividend, which can't run until this step stops blocking). `cancellable_trains`/`cancel_completed_choices`/`cancel_completed_route` in `step/route.rb`; surfaced via `entity_choices`, same bottom-panel mechanism as Finish/Cancel/pilot assignment. Keeps the step open one extra beat (`actions`/the auto-`pass!` in `finish_route` both now check `cancellable_trains`) only when there's a genuine undo option, so the common case (nothing to reconsider) still auto-advances with no extra click. *(PR4 follow-up, 2026-07-27)*
- [x] **Additional: completed-this-turn routes stay visible, color-coded** — `render_route_lines` (`assets/app/view/game/map.rb`) now also draws each of the current entity's already-finished routes for the turn (`Route#current_turn_routes`, a new opt-in hook alongside the existing `live_route_hexes`), reusing the same `ROUTE_COLORS`/`route_prop` polyline mechanism standard train games use for their own multi-route selection. Looked up via `round.steps.find { |s| s.respond_to?(:live_route_hexes) }` rather than `round.active_step`, since Route stops being the *active* step (Dividend/BuyTrain take over) well before the entity's turn actually ends, but its finished routes should stay drawn until it does. *(PR4 follow-up, 2026-07-27)*
- [x] **Additional: live cargo status in the ship selector** — `ship_rows`' `summary:` for the currently-flying ship now shows "X/Y holds used: <ore names>", updating every render as pickups happen, reusing `ShipSelector#render_row`'s existing `label — summary` rendering with no shared-file changes. Only surfaces for multi-ship entities (`ship_rows` itself already only renders then) — a single-ship entity has no selector row to attach it to. *(PR4 follow-up, 2026-07-27)*
- [x] **Bug fix: refueling station reusable within a single flight** — `move_to` refueled every time a ship's trace re-entered a station hex, with no memory of having already topped off there this flight (§7.11 caps it at once per flight). Fixed with `@refueled_hexes` (reset per trip in `launch_at`, not per OR), checked/updated alongside the existing `refueling_station_owner` check in `move_to`/`refuel!`. `hexes_in_range`'s BFS needed no equivalent fix — a path structurally can't productively revisit the same hex twice with equal-or-better remaining MP, so it was never actually exploitable there. *(PR5 follow-up, 2026-07-27)*
- [x] **Bug fix: Lucky's auto-chained tile popup missed Growth Corp ships** — `chain_hex_choice_popup?` checked `entity.id == 'LY'`, which only ever matches the bare LY minor itself, never a Growth Corp flying a ship with LY's inherited pilot -- so exploring with such a ship set `@pending_redraw` correctly (the choice *was* available) but the popup never auto-chained into view; an unrelated re-render (switching hexes/tabs) was needed before the already-available choice would show. Fixed to check `pilot_source_for_train` for the ship actually in flight, same as `move_to`'s own dispatch already does. *(PR7/PR8 follow-up, 2026-07-27)*

---

## Phase 5: Mine, Claim & Base Infrastructure

**Goal:** Implement persistent state objects for claims, bases, and refueling stations.

- [x] **5a. Asteroid tile state** — for each explored hex, track:
  - [x] Number of mines (1 or 2) and ore type per mine (N/I/R) — populated by `explore_hex!` from `MINE_DATA` *(PR4)*
  - [x] Unclaimed value and claimed value per mine — in `MINE_DATA` and copied into `@mine_state` on exploration *(PR4)*
  - [x] Whether there is a Used Mine marker on each mine this OR — `mine[:used]` flag; reset in `or_round_finished` *(PR3/PR4)*
  - [x] Which company (if any) owns a claim on each mine — `mine[:owner]`, set by `Game#place_claim!` *(PR5)*
  - [x] `@mine_state` hash on game object; pickup/claim logic looks up values dynamically *(PR4/PR5)*
- [x] **5b. Claims** — `G2038::Step::BuyInfrastructure`, wired into `operating_round` after `BuyTrain`: *(PR5)*
  - [x] Cost $60 first / $100 second per round by default (`Game::DEFAULT_CLAIM_COSTS`); per-corp overrides read from `claim_costs:` in `entities.rb` (RU: `[0, 100]` free-first; AL: `[60, 75, 100]`, 3 per round) *(PR5)*
  - [x] Must be within range — `Game#hexes_in_range(entity)`: BFS over hex neighbors from the entity's placed bases, bounded by its best owned ship's movement points, refueling at the entity's own stations along the way (mirrors how ships actually move in `Step::Route`, since §7.4's "range" is the same reachability question as "could a ship fly there") *(PR5)*
  - [x] Mark the mine as claimed by that company (`mine[:owner] = entity.id`) *(PR5)*
  - [x] **Displayed city revenue updates to the claimed value** on placement — `Part::RevenueCenter#revenue` is a plain hash keyed by phase color; set every key to the claimed value, mirroring how a plain-integer tile code already parses (see `Part::RevenueCenter#parse_revenue`) *(PR5)*
  - [x] Claims are permanent; cannot be removed or transferred (no code path does either) *(PR5)*
  - [x] Cannot place a claim on a mine that already has one — `mine[:owner]` checked in `choices` *(PR5)*
  - [x] Independents (minors) may place at most 2 claims total, ever, at the same $60/$100 tiers — enforced separately from corporations' per-round tier + lifetime cap, per §7.4's distinct wording for Independent Companies *(PR5)*
  - [x] Corporations' lifetime claim cap (Company/Corporation Summary table: TSI 10, RU 12, VP 5, LE 9, MM 6, OPC 7, RCC 8, AL 15) — `claim_limit:` in entities.rb, enforced in `claimable_this_round?` alongside the per-round cost tier *(PR5)*
  - [x] **Buying an Independent's claims** — confirmed with the user: claims only (not bases/refueling stations, which stay deferred), a flat $60 (not the escalating per-round schedule above, and not freely negotiated), available from Phase 2 on (same `after_phase_1?`/`can_buy_companies` gate as bases/stations) rather than Phase 1. `Step::BuyInfrastructure#buy_independent_claim_choices` offers a `buy_claim_` choice (aliased onto a bare hex click, same as any other claim) for every independent-held claim within `hexes_in_range`, gated on the buyer's own lifetime `claim_limit` (still counts against it, just not the per-round schedule/count) and `INDEPENDENT_CLAIM_PRICE` affordability; `Game#buy_claim_from_independent!` pays the price straight to the independent (not the bank) and reassigns `mine[:owner]`. Since `claims_placed_lifetime` is always computed fresh from current `@mine_state` ownership, the selling independent's own lifetime count automatically drops by one the instant the sale happens, freeing it to place a replacement claim later. One thing this surfaced worth double-checking live: an older draft of this note said the sale needed "the owner's consent" -- as built, it's a unilateral corp purchase at the fixed price with no seller-side confirmation step, matching what the user described this session; flag if that's wrong. *(implemented, post-Phase-11 addition)*
  - [x] **Buying an Independent's bases or refueling stations — confirmed not wanted.** Claims-only was the correct scope for this feature; the user confirmed this doesn't need to be built. *(decided 2026-08-02)*
  - [x] **Bug found while building the above: `after_phase_1?` reverted to false at Phase 5+** — `Phase#status` is set fresh from each phase's own `status:` array (`phase.rb`'s `@status = phase[:status] || []`), not carried forward, and PHASES only listed `status: ['can_buy_companies']` on phases 2-4, not 5/6. That silently broke base/station purchases (5c/5d) and inter-corp train buying (`can_buy_train_from_others?`) from Phase 5 onward too, not just this new claims feature -- a pre-existing bug, caught only because the new feature's own phase gate exposed it during testing. Fixed by adding the same `status:` entry to phases 5 and 6. *(bug fix, unrelated to but discovered via the claims feature above)*
- [x] **5c. Bases** — same step as claims: *(PR5)*
  - [x] Cost indexed by how many the corp has placed so far this game (`@base_hexes[entity].size`, lifetime, into `corp_data(entity)[:bases]`) — constant per corp in practice ($50 most, $25 Mars Mining), but written generically since not every per-corp array is constant (see 5d) *(PR5)*
  - [x] Placed on any explored asteroid tile with no claimed mine (`Game#can_place_base?`) *(PR5)*
  - [x] Flips tile to its gray base side (tile 2023, unlimited count) when placed; `@mine_state` entry deleted for that hex *(PR5)*
  - [x] Uses one of the corporation's existing (pre-allocated, $0-cost) `tokens:` slots for the physical placement — cost is charged separately via `entity.spend`, not via the token's own price *(PR5)*
  - [x] Mark hex as a route starting point — automatic: `deliverable_destination?`/`start_choices` already key off placed tokens *(PR4, reused)*
  - [x] Only one base may be placed per round per company — `@base_placed` flag in the step, reset each OR *(PR5)*
  - [x] **Lifetime cap on total bases** — capped at the length of `corp_data(entity)[:bases]` (`Game#base_limit`, mirroring `claim_limit`'s pattern), enforced in `BuyInfrastructure#base_choices`. Originally shipped uncapped as a simplification; revisited after live play surfaced two real problems this caused: (1) RU kept re-entering the Place Base sub-phase every OR after its one intended base, and (2) `Game#place_base!` laid the gray '2023' tile via a bare `@tiles.find`/`hex.lay` with no pool replenishment — since '2023' is an `'unlimited'`-count tile (`init_tile` only ever pools a single instance for those), the *very first* base placed anywhere in the game consumed that sole instance and every later one crashed (`hex.lay(nil)` → `NoMethodError: undefined method 'cities' for nil`). Fixed by calling the engine's existing `update_tile_lists(tile, old_tile)` before `hex.lay` (same call `Step::Tracker` makes for any normal tile upgrade), which duplicates unlimited tiles back into the pool on each use. *(PR5, revisited 2026-07-26)*
- [x] **5d. Refueling stations** — same step: *(PR5)*
  - [x] Cost indexed the same way as bases (`@station_hexes[entity].size`, lifetime) into `corp_data(entity)[:stations]` — this one genuinely isn't constant per corp (OPC: `[0, 50, 50]`, first free); verified OPC's 1st station is $0 and 2nd is $50 *(PR5)*
  - [x] Must be placed at a base hex without one yet, **within range of the placing corporation** — explicitly allowed on a base owned by a *different* corporation per §7.42's own wording (`Game#can_place_station?` only checks base presence, not ownership) *(PR5)*
  - [x] Grants +3 MP (capped at the ship's own movement allowance) to ships of the owning corporation — applied in `Step::Route#move_to` via `refuel!`, and mirrored in `hexes_in_range`'s BFS so placement range-checks account for it too *(PR5)*
  - [x] Only one refueling station per base — `refueling_station_owner(hex.id)` checked in `choices` *(PR5)*
  - [x] **Lifetime cap on total stations**, same mechanism as bases (`Game#station_limit`, `corp_data(entity)[:stations].size`) — applied for consistency with the base fix rather than a separately reported bug *(revisited 2026-07-26)*

---

## Phase 6: TSI Probe

**Goal:** Model TSI's special probe mechanics.

- [x] **6a. Probe entity** — probe is removed from depot in `setup`; `@probe.buyable = false`; given to TSI immediately in `setup` (not on float, since it must be flyable before TSI floats) *(PR4/PR6)*
- [x] **6b. Probe operation** — per the rules (§6 OR sequence), TSI gets an OR turn *after all six independents operate and before other corporations*, as long as it hasn't floated yet: `operating_order` inserts TSI right after the floated minors. `acting_for_entity` redirects TSI's pre-float turn to the ST private's owner (TSI has no real president yet). Exploration bonuses ($10/hex) go to ST's owner personally while TSI is unfloated (`probe_bonus_recipient`), not to TSI's treasury; no revenue earned (`trace_revenue` already zeroes out the Probe) *(PR6)*
- [x] **6c. Probe retirement** — per the rules/Let's Play sheet, the Probe obsoletes when **Phase II begins** (anyone buying a 4/3 or 6/2), not specifically when TSI buys a train. This needed **no custom code**: the Probe's `TRAINS` entry already had `rusts_on: %w[4/3 6/2]` (since PR0), which the engine's standard `rust_trains!` mechanism already handles automatically on every train purchase, by anyone. An earlier `after_buying_train` override that rusted the Probe on TSI's *first* train purchase (any type) was removed -- it was both redundant with the existing `rusts_on` field and wrong (would have retired the Probe as soon as TSI bought an ordinary Phase I ship, likely still in Phase I). ST's own closure (`close: { when: 'bought_train', corporation: 'TSI' }`, fires whenever TSI buys *any* spaceship) is a separate, correct, and unrelated rule -- unaffected by this fix. *(PR6, corrected same PR)*

**Note on reachability of 6b:** TSI's four linked privates hold exactly 50% of its shares (ST=20% president share + RS/VA/TS=10% each), and the initial auction always sells them in strict order 7→8→9→10, so ST (10) is provably always the *last* of the four sold. That means buying ST is the exact transaction that crosses TSI's 50% float threshold -- `SharePool#buy_shares` fires `float_corporation` synchronously as part of that same purchase, before any Operating Round can exist. In other words, **TSI can never actually be unfloated while ST is player-owned** in this implementation; 6b's `operating_order`/`acting_for_entity`/`probe_bonus_recipient` overrides are unreachable under normal play. Kept intentionally as a defensive safety net (e.g. in case a future variant changes the share split or auction order), not because a live bug was found.

---

## Phase 7: Independent Company Special Abilities

**Goal:** Wire up each independent's unique ability.

- [x] **Fast Buck (FB)** — $15/round added to treasury, implemented in `G2038::Round::Operating#setup` *(PR2)*
- [x] **Ice Finder (IF)** — $10 bonus per Ice ore delivered (`Game#independent_ore_bonus`, paid in `trace_revenue`); must draw a second tile when exploring if the first tile drawn has no Ice mines. **No player choice** — the second (borrowed) tile is placed automatically since it was only drawn because the first one already failed the ore check (`Game#needs_second_draw?`/`#peek_tile`/`#borrow_second_tile`/`#resolve_second_draw!`, orchestrated by `Step::Route#move_to`/`#auto_redraw!`) *(PR7)*
- [x] **Drill Hound (DH)** — $10 bonus per Rare ore delivered; same automatic second-draw mechanic, gated on Rare mines instead of Ice *(PR7)*
- [x] **Ore Crusher (OC)** — $10 bonus per Nickel ore delivered *(PR7)*
- [x] **Torch (TH)** — all Torch spaceships get +1 movement point (`Game#ship_distance`, read by `Step::Route#mp_left` and `Game#hexes_in_range` instead of `train.distance` directly) *(PR7)*
- [x] **Lucky (LY)** — draws 2 tiles every time it explores, **then the player chooses which to place** (the one real player-facing choice among the second-draw powers) — `Step::Route#start_redraw!`/`#resolve_redraw!` via a `@pending_redraw` sub-state and `redraw_` choice keys; unconditional since `needs_second_draw?` is always true for LY *(PR7)*
- [x] **Verify live in-browser**: confirmed working by the user (2026-08-02) *(PR7 follow-up)*
- [x] **Pilot bonuses** — when an independent forms a Growth Corporation or merges into the AL, its pilot certificate transfers to the new corporation (`@growth_corp_pilot`, appended in both `form_growth_corporation!` and `merge_independent_into_al!`) and provides the same bonus to one assigned spaceship per OR (`Step::Route`'s per-OR `@pilot_assignments`, `Game#pilot_source_for_train`) — a corp holding multiple pilots (AL, via repeated mergers) assigns each independently to its own ship. Also gated off entirely from Phase 5 on (`pilot_source_for_train` returns nil once `phase.name.to_i >= 5`, matching the private closing at that point). *(PR8, checkbox was stale)*

---

## Phase 7b: Corporation Home-Base Delivery Bonuses

**Goal:** Wire up each corporation's `delivery_bonus:` field (entities.rb) — currently inert data, read nowhere. Distinct from Phase 7's independent bonuses above (those pay *that independent* per ore *it* delivers, anywhere) -- these instead pay a bonus at a fixed *hex* (the named corp's home base) to **whichever entity delivers the matching ore there**, not just the home corp itself. E.g. if MM's home base pays an Ice bonus, an Independent or a different corp delivering Ice there also collects it.

- [x] **Venus Prospectors (VP) home base** — +$20/Rare ore for whoever delivers there *(PR7b)*
- [x] **Lunar Enterprises (LE) home base** — +$20/Nickel ore for whoever delivers there *(PR7b)*
- [x] **Mars Mining (MM) home base** — +$20/Ice ore for whoever delivers there *(PR7b)*
- [x] **Outer Planet Consortium (OPC) home base** — +$10/Ice ore for whoever delivers there — corrected from an earlier `:N` (Nickel) placeholder in entities.rb, confirmed Ice per the user *(PR7b)*
- [x] **Ring Construction Corporation (RCC) home base** — +$10/Nickel ore for whoever delivers there *(PR7b)*
- [x] TSI and AL have no home-base delivery bonus (confirmed with user) *(PR7b)*
- [x] Bonus amounts confirmed with user: `delivery_bonus_amount:` added to entities.rb alongside each corp's existing `delivery_bonus:` ore symbol *(PR7b)*
- [x] Stacks with normal cargo/independent-ore revenue (not a replacement), paid by the bank — folded directly into `Game#trace_revenue` alongside `independent_ore_bonus`, same mechanism (the combined figure becomes the route's revenue, disbursed via the standard OR dividend flow) *(PR7b)*
- Implementation: `Game#home_delivery_bonuses` builds a `{hex_id => [ore, amount]}` lookup once from each corp's own `coordinates:` (its fixed home hex, not `base_hexes` — a corp's *later*-placed bases don't carry the bonus) via `Game#home_delivery_bonus(delivery_hex, cargo)`, called from `trace_revenue` with `trace.last` as the delivery hex.

---

## Phase 8: Growth Corporation Formation

**Goal:** Allow independents to convert into Growth Corporations -- i.e. a second way to start any unfloated corp (TSI/RU/VP/LE/MM/OPC/RCC, not AL) besides the normal cash-par path: a player trades in one of their own still-active independents for that corp's president's certificate instead of paying cash.

- [x] **8a. Trigger** — surfaced directly in the existing par UI (`assets/app/view/game/par.rb`), not a separate step/prompt: clicking an unstarted corp shows the normal par-price buttons *plus* one "Exchange X" button per independent the player owns, via `G2038::Step::BuySellParShares#growth_exchange_choices(player, corporation)`. (An earlier attempt used a standalone non-blocking `choose`-action step; reverted after it crashed `assets/app/view/game/round/stock.rb`'s `choice_available?` call, which only ever checks whatever `round.active_step` resolves to -- and `active_step` (no-arg) only ever considers *blocking* steps, so a deliberately non-blocking step could never satisfy it. Subclassing `BuySellParShares` instead means the growth-exchange action always travels with whichever step is already legitimately active.) Only available in Phases 2-3, and never once the Asteroid League has formed (`Game#growth_conversion_allowed?`, checked by `growth_convertible_corporations`) *(PR8)*
- [x] **8b. `step/buy_sell_par_shares.rb`** — the step itself just validates and dispatches to `Game#form_growth_corporation!`, which handles the whole conversion: *(PR8)*
  - [x] Player receives the corp's president's certificate via `share_pool.buy_shares(player, corp.presidents_share, exchange: :free)` — no cash paid, mirroring `form_asteroid_league!`'s existing precedent *(PR8)*
  - [x] Independent's spaceship(s) transfer directly (`train.owner = corp; corp.trains << train`) *(PR8)*
  - [x] Independent's remaining cash transfers (`minor.spend(minor.cash, corp)`) *(PR8)*
  - [x] Independent's claimed mines reassign to the corp (`mine[:owner] = corp.id`) — these DO count against the corp's own `claim_limit`, since `claims_placed_lifetime` is always computed fresh from `@mine_state` *(PR8)*
  - [x] Independent's home-base token transfers via `Token#swap!` as an **extra**, uncounted base — never pushed into `@base_hexes[corp]`, so `base_limit`/`base_cost` (both keyed off that array's size, not `entity.tokens`) don't count it *(PR8)*
  - [x] Corp always pars at exactly $67 (`stock_market.set_par` with the existing `67p` cell) but its market token starts at the separately-labeled $10/`par_2` cell (`game.rb`'s `MARKET`/`MARKET_TEXT` already had this pre-labeled "All Growth Corps Par") — `corp.share_price` is reassigned there directly after `set_par`. The corp's remaining 8 shares stay in its own treasury (`ipo_owner` defaults to the corp itself); as long as they're never re-parred, `Share#price_per_share` keeps pricing them off `par_price` ($67) forever regardless of where `share_price` (the market token) later moves — verified via script: after moving the market token to $34, a treasury share purchase still paid the corp exactly $67, bank untouched *(PR8)*
  - [x] `corp.capitalization = :incremental` set before granting shares, so treasury purchases pay the corp directly (`SharePool#buy_shares`' receiver logic) and the automatic `float_corporation` lump-sum payout is skipped; `corp.floated = true` set directly instead (same pattern as `form_asteroid_league!`) *(PR8)*
  - [x] Independent's special ability transfers as an assignable **pilot** — `@growth_corp_pilot[corp.id]` is an array of source ids (usually one entry, but a corp that absorbs multiple independents over time, e.g. AL via Phase 9 mergers, can accumulate more than one), read by `independent_ore_bonus`/`ship_distance`/`needs_second_draw?` (all now pilot-aware) whenever the corp has 2+ ships; see 8b-pilot below *(PR8)*
  - [x] Independent (and its matching, previously-dangling private company of the same sym — confirmed via code archaeology that `waterfall_auction.rb#buy_company` never closes it) are both closed (`minor.close!`, `company_by_id(minor.id)&.close!`) *(PR8)*
  - [ ] **Release the independent's reserved AL share** — since it's converting to a Growth Corp instead of ever merging into the AL, `form_growth_corporation!` must un-reserve (not grant) `@al_reserved_shares[minor.id]` (see 9a2 below), so that 10% becomes normal buyable AL stock. This must work regardless of whether the AL has even formed yet, since Growth Corp conversion (Phases 2-3) always finishes before `growth_conversion_allowed?` allows it once `@asteroid_league_formed` — i.e. this can fire before AE's owner ever declares AL. *(PR9, since it depends on 9a2's reservation existing first)*
- [x] **8b-pilot. Per-ship pilot assignment** — mirrors 1822's Pullman-to-train attachment (`g_1822/step/route.rb`): `Step::Route` tracks `@pilot_assignments` (a `{pilot_source => Train}` hash, reset every OR in `setup` since a fresh step is built each round). **Each inherited pilot is assigned to its own distinct ship — never shared, never doubled up on one ship** (corrected after an initial wrong assumption that all of a corp's pilots shared one assignment slot). `pilot_choices` offers a choice only when there's real ambiguity — one pilot contending for 2+ un-run ships, or (its mirror image) 2+ pilots contending for the one ship left — auto-assigning (via `pilot_source_for_train`, and at the very start of the turn via `setup`'s `resolve_unambiguous_pilots!`) the one truly unambiguous case: exactly one pilot, exactly one ship. Every assignment (auto or explicit) is announced once, via the shared `assign_pilot!` helper — `"<Corp>: Pilot <Name> (<source>) assigned to <ship>"` — and `ship_label` appends the pilot's two-letter source code to its ship's label everywhere (ship selector, route summaries, this same log line) once assigned, so it's always visible which ship (if any) a pilot is riding. `Game#pilot_source_for_train(entity, train)` returns which specific pilot (if any) is assigned to a given ship, found by looking up the `Route` step directly by type rather than via `round.active_step` (which recomputes every step's `blocking?`, and for `BuyInfrastructure` that chains through `claim_choices` -> `hexes_in_range` -> `ship_distance` -> this same method — infinite recursion, the identical documented pitfall `Game::Base#ability_right_time?` already routes around via `ability_blocking_step`) *(PR8; announcement/labeling + 2-pilots-1-ship choice added 2026-07-27)*
- [x] **8c. Corporation group unlocking** — `after_par` in game.rb fires `event_group_b/c_corps_available!` once all corps in the prior group have IPO'd; Groups A/B/C/D are correctly partitioned in `setup`. *(PR4)*
- [x] **8d. Growth Corp stock round behavior** — covered by 8b's treasury-at-`ipo_owner`/`capitalization: :incremental` mechanism: shares bought from a Growth Corp's own treasury pay the corp (verified via script); normal corps are unaffected since this is only ever set on a corp actually formed via `form_growth_corporation!` *(PR8)*
- [x] Cleaned up `entities.rb`'s dead `exchange` ability (present on all 6 minors, wrong corp list — had AL, missing TSI — and superseded by the dedicated step above) and the stale `# TODO Phase 2: remove exchange ability` comments on the matching privates *(PR8)*
- [x] **Per-ship pilot assignment confirmed working live** by the user (2026-08-02). Formation and treasury share pricing weren't separately called out — presumed fine since Growth Corps have been forming and playing normally, but not explicitly confirmed on their own. *(PR8 follow-up)*

---

## Phase 9: Asteroid League Formation

**Goal:** Model the AL formation, analogous to the Prussian in 1835.

Reference: `g_1835/step/form_prussian.rb` and `step/merge_to_prussian.rb`.

- [x] **9a. Formation trigger (event)** — `event_asteroid_league_can_form!` fires when a 5/4 or 7/3 is purchased; adds AL to `@corporations` and logs the announcement. *(PR4)*
- [x] **9a2. Reserve one AL share per independent** — `Game#setup` marks 6 of AL's 8 non-president treasury shares `buyable = false` (explicitly excluding the president's share itself — `find { |s| s.buyable && !s.president }`, a bug caught in testing: `find(&:buyable)` alone can grab the president's cert since it's buyable too) and records `@al_reserved_shares` (`{minor.id => share}`), one distinct share per independent. AL's other 2 non-president shares stay normally buyable from the start. `form_growth_corporation!` releases (`share.buyable = true`) rather than grants the converting independent's reserved share — it's closing permanently and will never merge in, so its seat frees up as ordinary market stock. Verified via script: 6 shares reserved, the other 2 (+ president's) stay buyable; converting a minor via Growth Corp releases its reservation without granting it to anyone. *(PR9)*
- [x] **9b. AL formation** — already fully implemented pre-Phase-9 (`Game#form_asteroid_league!`, triggered via `G2038::Step::SpecialChoose`'s `choose_ability` on AE): pars at $125, $250 capital, president's cert to AE's owner, force-floated immediately. No separate `step/form_asteroid_league.rb` was ever needed. *(PR6)*
- [x] **9c/9d. Voluntary + recurring mergers** — new `G2038::Step::MergeIntoLeague` (`step/merge_into_league.rb`), modeled directly on 1835's Prussian merger step (`g_1835/step/minor_exchange.rb`, confirmed by direct reading — no dedicated Action class, reuses `Action::Choose`; a `round_state`-backed `declined` array that resets every new round instance for free). Confirmed timing with the user: the instant AL forms, every remaining independent gets one offer, clockwise **from the AL president** (not necessarily whoever formed it) — in whichever round type AL happened to form in. After that, only independents who've *never* been offered a choice may be offered one during a Stock round (the one true initial pass, tracked via a permanent `@al_independents_ever_offered` id list — not round-scoped); anyone who already declined is only re-offered at the start of an Operating round, every OR, for as long as they remain. Overrides `active?` entirely (bypassing the Passer/`@passed` default), same reason 1835's step does: this step's relevance can flip from false to true *partway through* a round (AL can form mid-OR), well after `skip_steps` may have already marked it passed while nothing was yet eligible — a purely dynamic `active?` (`!active_entities.empty?`) lets it reactivate later in that same round instead of staying stuck. Wired as the first step in both `stock_round` and `operating_round`. Rotation is a stable sort of remaining independents by owner's position in `@game.players` rotated to start at the AL president — same-owner independents stay adjacent, so one player is asked about all of theirs back to back. `Game#merge_independent_into_al!(minor)` does the actual transfer (½ treasury cash to owner, the *other* half to the AL's own treasury — initially shipped only giving the owner's half and letting the rest vanish with the closed minor, caught and fixed after the user asked to confirm this specific behavior — the AL-bound remainder is computed as "whatever's left after the owner's floor-divided half" rather than a second independent division, so odd amounts split correctly; the reserved AL share via `share_pool.buy_shares(..., exchange: :free)` — note `share.buyable` must be explicitly flipped `true` first, `buy_shares` doesn't do it automatically, another bug caught in testing — ship(s), base as an extra uncounted token per Phase 8's pattern, claims as a plain ownership reassignment, pilot appended to `@growth_corp_pilot['AL']`, then closes the minor and its matching private). Verified via script: rotation order/adjacency, SR-excludes-already-offered vs. OR-always-reoffers, and the full merge transfer (cash split both ways, correct specific share, ships, closure). *(PR9)*
- [x] **9e. Mandatory merger at Phase 5** — new `independents_must_join_league` event on the 7/6 train (phase 5's trigger), `Game#event_independents_must_join_league!` force-loops `remaining_independents` directly into `merge_independent_into_al!`, bypassing the voluntary step entirely (mirrors 1835's `event_forced_pr_exchange!`). AL is guaranteed to already exist by Phase 5 (Phase 4's `asteroid_league_must_form` event always runs first). Verified via script: a mixed scenario (one independent already Growth-Corp-converted, five still remaining with real ships) sweeps cleanly, transfers all 5 ships, leaves the converted one's released share untouched. *(PR9)*
- [x] **9f. Mandatory merger on bankruptcy** — discovered during implementation that the base engine's generic "must buy a train or go bankrupt" mechanism (`Game::Base#must_buy_train?`, `MUST_BUY_TRAIN = :route` default) is effectively inert for G2038 already, since it keys off `@graph.route_info(entity)` and this game has no real path-based route graph — so this needed its own G2038-specific trigger rather than hooking the generic one. `Game#independent_must_merge?(minor)`: true once AL exists, for an independent with zero ships that can't afford the cheapest remaining Depot ship. Checked in `G2038::Round::Operating#skip_entity?` (overridden to call it whenever the base `closed?` check doesn't already skip the entity) — merges it in place of letting it take a turn. Safe to call more than once per entity: once merged, the `closed?` short-circuit in `super` prevents ever re-checking `independent_must_merge?` again, so no double-merge risk from `any_to_act?`/`start_operating` both probing `skip_entity?`. Verified via script, including the double-call safety check. *(PR9)*
- [x] **9g. AL ship limits** — `G2038::Step::BuyTrain#other_trains` excludes AL's last remaining ship from ever being purchasable by anyone once `@al_corporation.trains.one?`. Verified via script: AL's sole ship is excluded; adding a second makes both available again. *(PR9)*
- [x] **9h. AL reserve bases/claims** — `base_limit`/`claim_limit` (game.rb) subtract `remaining_independents.size` (bases) / `remaining_independents.size * 2` (claims) from AL's own fixed cap specifically, clamped at 0 — AL's *own new-placement* capacity shrinks while independents remain unmerged and grows back as each merges in. Does not affect bases/claims an independent brings *with* it on merger (those transfer as-is, same as Phase 8's inherited-base pattern) — only AL's own fresh `BuyInfrastructure` placements. Verified via script: AL's base/claim limits shrink/grow correctly as `remaining_independents` changes; a normal corp's own limit is untouched. *(PR9)*
- [x] **Verify live in-browser** — AL formation and independent mergers confirmed working by the user (2026-08-02). *(PR9 follow-up)*
- [ ] **Still needs testing: buying AL's own stock** — specifically the mechanic of a player purchasing an AL share once it's buyable (either one of AL's initially-unreserved shares, or a formerly-reserved share released as its independent merges in) hasn't been exercised live yet.

---

## Phase 10: Private Company Special Abilities (Post-Auction)

**Goal:** Wire up TS, VA, RS special abilities when owned by corporations.

Revised from the original "once per OR" framing after confirming with the user: each ability is **one-time use**, exercisable in any Buy Companies step from acquisition until Phase 5 begins (whichever comes first), and is an **extra, uncounted** placement — it never touches the corp's normal base/station/claim lifetime cap, same as a Growth Corp's inherited base (Phase 8).

- [x] **Tunnel Systems (TS)** — once, any time before Phase 5: free base on *any* explored, unclaimed hex (map-wide, not range-limited — the one real difference from VA/RS) *(PR10)*
- [x] **Vacuum Associates (VA)** — once, any time before Phase 5: free refueling station within range of the owning corp's spaceships *(PR10)*
- [x] **Robot Smelters (RS)** — once, any time before Phase 5: free claim within range of the owning corp's spaceships *(PR10)*
- [x] Replaced the placeholder `shares`-only abilities in entities.rb with an added `{ type: 'generic', subtype: 'free_base'/'free_station'/'free_claim', when: 'owning_corp_or_turn', count: 1, remove: '5' }` each — `Ability::Generic` is an existing shared engine escape hatch (already used elsewhere, `g_rolling_stock_stars/entities.rb`) that lets `subtype:` become the ability's real `.type`, so no new `Ability` subclass or shared file was needed. `count: 1` (one-time use, self-removes via `ability.use!`) and `remove: '5'` (auto-stripped the instant Phase 5 begins, via the engine's existing `Abilities#remove_ability_when`) are both built-in `Ability::Base` mechanisms — no custom tracking hash *(PR10)*
- [x] **Timing constraint met via the step itself**, not the ability's `when:` — `when: 'owning_corp_or_turn'` only restricts to "during this corp's own OR turn" (checked by the pre-existing generic `ability_right_time?`/`corporation_owned_ability?`), so the *step-level* restriction comes from the fact that the choose action is only ever wired into `G2038::Step::BuyCompany` (`step/buy_company.rb`, subclassing `Engine::Step::BuyCompany` the same way `BuySellParShares` was subclassed for Phase 8) and nowhere else — not Route, not BuyInfrastructure. Doesn't apply to ST's Probe-flying privilege, a fundamentally different kind of ability *(PR10)*
- [x] **Uncounted extra**: base/station skip pushing to `@base_hexes`/`@station_hexes` (`place_base!`/`place_station!`'s new `free:` kwarg); a free claim gets `mine[:free] = true`, and `claims_placed_lifetime` excludes those — but `claim_hexes` (the corp card's claim-flag display) still shows it, verified via script *(PR10)*
- [x] Verified via script: ability correctly absent before an operating round exists; TS's free base reaches a hex outside `hexes_in_range` while VA/RS correctly don't; using the ability consumes it (gone from `@game.abilities(entity, ...)` immediately after) while leaving `base_hexes`/`station_hexes`/`claims_placed_lifetime` untouched; an unused ability persists correctly into a later OR; `remove_ability_when('5')` strips an unused ability *(PR10)*
- [x] **Verify live in-browser** — confirmed working by the user (2026-08-02) *(PR10 follow-up)*

---

## Phase 11: Endgame & Scoring

**Goal:** Implement bankruptcy detection and final scoring.

- [x] **11a. Bank depletion** — confirmed exact rule with the user: wherever the bank breaks, finish the round-pair currently in progress (rest of the current OR set if triggered mid-OR, or the current SR plus the OR set right after it if triggered mid-SR), never starting a new SR afterward. Traced `Game::Base`'s existing `end_now?`/`final_or_in_set?` against this precisely and the **default** `GAME_END_CHECK = { bankrupt: :immediate, bank: :full_or }` already implements it exactly — no override needed. Verified via script: triggering mid-final-OR ends immediately at that OR; triggering mid-earlier-OR keeps playing to the set's last OR; triggering mid-SR doesn't end until the OR set that naturally follows. *(PR11)*
- [x] **11b. Bankruptcy (corporations only)** — confirmed with the user this applies to corporations only; independents are governed entirely by Phase 9f (merge into AL once it exists; before that, a broke independent with no ship simply sits idle, no consequence). `Game#must_buy_train?` override (`return false if entity.minor?`) replaces the base engine's default (`MUST_BUY_TRAIN == :route`, checking `@graph.route_info` — permanently inert for G2038 since there's no tile-path graph). Everything downstream already works generically with no changes: `Engine::Step::BuyTrain`'s EMR flow, `can_go_bankrupt?`/`total_emr_buying_power`, `declare_bankrupt`, and the already-default `GAME_END_CHECK[:bankrupt] = :immediate` + `BANKRUPTCY_ENDS_GAME_AFTER = :one` (ends the game the instant one player goes bankrupt — already exactly the rule, no override needed). Verified via script: a corp with 0 ships and depot ships available is correctly flagged `must_buy_train?`, an independent in the identical situation is not. *(PR11)*
- [x] **11b2. President funding restricted to the true cheapest ship** — the base engine's `EBUY_DEPOT_TRAIN_MUST_BE_CHEAPEST` forces any president-assisted purchase to be strictly the single cheapest train in the depot. Confirmed with the user this should only bind when the corporation could have afforded that cheapest ship on its own treasury (`buying_power`, no assistance) -- if it genuinely can't afford anything, president funds may go toward *any* currently purchasable ship at any price, not just the cheapest. `G2038::Step::BuyTrain#check_for_cheapest_train` skips the restriction (`super`) precisely when `buying_power(current_entity) < @depot.min_depot_train.price`; when the corp *can* afford the cheapest, that specific purchase never needs assistance in the first place (`remaining <= 0`), so the only case this check still fires for is "afford the cheapest, but try to use the president's money to jump straight to something pricier" -- which stays correctly blocked. Verified via script: a broke corp may buy a pricier train with assistance; a corp that could afford the cheapest is still blocked from doing the same. *(post-Phase-11 fix)*
- [x] **AE certificate-limit exclusion** — confirmed with the user: once the AL forms, AE stops counting toward its owner's certificate limit, even though AE itself doesn't actually close until AL buys its first ship (a separate, later trigger). `Game#num_certs` override subtracts 1 whenever `@asteroid_league_formed` and the entity still holds AE (`CERT_LIMIT_INCLUDES_PRIVATES`, the base default, otherwise counts every held private uniformly with no per-company distinction). Verified via script. *(post-Phase-11 fix)*
- [x] **11c. Final scoring**: *(PR11)*
  - [x] Each player's score = cash on hand + stock portfolio value (at current stock prices) + face value of any Private Companies still held (if game ends before Phase 5) — `Player#value`'s default formula already provides cash + share prices + companies' face value out of the box; `Game#player_value` override subtracts companies' value once Phase 5+ is reached (TS/VA/RS/PI persist as held assets after their one-time ability is stripped by `remove: '5'`, but shouldn't count toward score that late). Verified via script: counts before Phase 5, excluded at Phase 5+. *(PR11)*
  - [x] Assets held in company/corporation treasuries do not count for players — already true by construction, `Player#value` never reaches into a corporation's own cash/treasury shares; no change needed. *(PR11)*
  - [x] AL shares count at AL's current stock price — verified via script with no special-casing needed: AL is just a normal corporation with a `share_price` once formed, already priced into `Player#value`'s existing share-sum term identically to any other corp. *(PR11)*
  - [x] "Spreadsheet" mode for final OR — `assets/app/view/game/spreadsheet.rb` (`View::Game::Spreadsheet`) is a generic, always-available tab in every game (player cash/value/shares/certs plus a full corporation table); this already satisfies the requirement with zero new code, no G2038-specific gating found that would hide it. *(PR11)*
- [ ] **Verify live in-browser** — all of 11a/11b/11c verified at the data layer via script but not yet exercised through a real browser session reaching the actual endgame. As of 2026-08-02 no live game has yet run to conclusion without hitting a bug first (this session's fixes may clear the way). *(PR11 follow-up)*

---

## Phase 12: Cosmetic / Visual Polish

**Goal:** Custom art, not gameplay changes — none of this affects rules, state, or scoring. Requested by the user; not yet started on any item.

- [ ] **Blue (unexplored) hex art** — currently rendered via the generic engine tile-color system (plain blue hex, no custom art). Wants an "open space" look: a starfield backdrop with tiny speckled stars. Likely a custom background image/pattern for the blue tile color, or a dedicated unexplored-hex tile graphic — needs a design pass to figure out the right hook (`assets/app/view/game/hex.rb`/`part/*` rendering vs. a static per-hex-color background image).
- [ ] **Mine (asteroid) tile art** — the 2001–2022 tile set currently uses the engine's generic tile rendering (plain colored hex + city/revenue circles for the mine slots), no custom art. Wants these to look like asteroids superimposed on the same starfield backdrop as the unexplored blue hexes above (so an explored mine tile still reads as "space" underneath the asteroid art).
- [ ] **Base tile art** — bases currently render via tile `2023` (the generic gray "base" tile flip in `place_base!`, `game.rb`), with no custom art distinguishing a base from any other gray tile. Wants these to actually look like a base/station structure.
- [x] **Ship icon should reflect the actual ship being flown** — done (2026-08-04). Real ship illustrations extracted from the printed materials (`2038_Expansion_Set.pdf` for I/III/V/VI ships, a user-provided scan for the Probe/II/IV ships), made transparent, and composited with an MP badge into a full per-ship, per-remaining-MP marker set (96 SVGs, `public/icons/g_2038/ship_marker_<key>_<mp>.svg`, `Step::Route::SHIP_MARKER_ART`) — every one of the 12 real ship types (Probe included) now has its own art, no fallback to the old generic marker left. Also reworked to render as a top-level map overlay (`Map#render_ship_marker`, `Step::Route#ship_marker`) instead of the small-icon tile-slot system, so it can be bigger, is never clipped/repositioned to dodge other icons, is click-transparent (`pointer-events: none`), and positions itself (dead-center for double-mine hexes, below-center otherwise) based on hex content.
  - [ ] **Future enhancement, not scoped for now**: further customize each ship's icon to show a representation of its cargo holds -- how many are filled, and perhaps even color-coded by ore type (e.g. red/green/blue for the three ore types) -- so the marker conveys cargo state at a glance, not just remaining MP. Parked per the user (2026-08-02).
- [x] Exploration-undo warning log — tracked above as Phase 4i, not duplicated here.
- [ ] **"Show Last Route and Tile" doesn't work for G2038** — the shared control (`assets/app/view/game/map_controls.rb`'s `route_controls`/`generate_last_route`) draws colored segments along printed track lanes via `Engine::Route#connection_hexes`/`#halts`/`#node_signatures`, all track-connectivity concepts. G2038's routes are built from a plain `hexes:` list (never `connection_hexes:`/`routes:`/`nodes:`), so those all silently degrade to empty arrays and the control draws nothing — no crash, just quietly broken. Since a trackless game has no lanes to draw a line along in the first place, the existing control isn't fixable as-is; for now it's hidden entirely for any `HIDE_TILE_TRACK` game (found live in browser; fixed by returning `''` early from `route_controls`). A real fix would be a G2038-specific replacement reusing the existing live-in-flight hex-highlight overlay (`@round.laid_hexes`, already working for the *current* ship's trace) to show every hex each of the entity's ships visited last OR — needs `@round.laid_hexes` to actually *accumulate* across every ship flown that turn rather than reflecting only the most recent one (untested whether it already does; `Dividend#process_dividend` snapshots whatever `@round.laid_hexes` holds at payout time into `OperatingInfo#laid_hexes`, and `update_trace_highlight` currently just replaces it per-ship, `@round.laid_hexes = @trace.dup`, so this likely needs its own fix first). Parked, not started.

---

## Phase 13: Optional Rules — Short Game & Expansion Set

**Goal:** Two opt-in variants, both already registered as selectable `OPTIONAL_RULES` in `meta.rb` (`:optional_short_game`, `:optional_variant_start_pack`) with real descriptions, but each only partially wired into `game.rb` so far. Source: `2038_rules.txt` (a plain-text export the user provided after the PDF turned out to be unreadable — image-based, no text layer, and this machine has no `pdftoppm`/poppler-utils for the Read tool's page-image fallback) for 13a below; `2038_Expansion_Set.pdf` (has a real text layer, read directly via `pdftotext`) for 13b/13c. Nothing in this phase is implemented yet beyond what's noted as already-wired.

- [ ] **13a. Short Game** (`optional_short_game`) — the rules' own "Short Game Rules Summary" section gives the complete, authoritative list (five changes total, confirmed against the full rules text, not just the two-line summary in `meta.rb`):
  1. Remove $6,000 and 2 Phase II ships from the game. **Bank cash is already correct** — `BANK_CASH = 10_000`, and `bank_starting_cash` returns exactly `4_000` under this flag (10,000 − 6,000 = 4,000, confirmed matching). **Removing 2 Phase II ships (`4/3`) is not wired** — nothing conditionally reduces `TRAINS`' `num:` for `4/3` (or its `6/2` variant) based on this flag.
  2. TSI's initial stock/par value is $67 (not $100) and its treasury receives $670 (not $1,000) — **already correct**, `after_buy_company`'s `optional_short_game ? 67 : 100` matches exactly.
  3. **The two Group C corporations, Outer Planet Consortium (OPC) and Ring Construction Corp (RCC), are not used at all in the Short Game** — **not wired**; both are unconditionally present in `CORPORATIONS` today with no short-game exclusion anywhere.
  4. **Certificate limits are reduced** — **not wired**; `CERT_LIMIT` is a single fixed hash (`{ 3 => 22, 4 => 16, 5 => 13, 6 => 11 }`, matching the *Full Game* column in both the base rules and the Expansion doc's own repeated table) with no short-game variant. The rules text says "see chart" for the actual reduced numbers, but the chart itself didn't extract cleanly from the plain-text export — the real short-game cert-limit numbers still need to come from the user or a cleaner source.
  5. **A real, previously-unknown rule difference, not just a "not wired yet" gap**: in the Full Game, an independent whose owner merges it into the AL *after* the AL's initial formation still gets half its treasury cash if it still owns a ship at merge time (§8.11, "only in the Full Game and only if it still has a spaceship when it joins") — exactly the behavior this session already built and verified (`merge_independent_into_al!`'s `first_opportunity || !minor.trains.empty?` check). **In the Short Game specifically, that "still owns a ship" exception doesn't exist at all** — a later-joining independent's owner gets nothing regardless of ship ownership; only merging at the AL's *first* opportunity ever pays out. `merge_independent_into_al!` needs a short-game-aware branch: `owner_gets_half = first_opportunity || (!optional_short_game && !minor.trains.empty?)`, or equivalent.

- [ ] **13b. Adding the New Corporations** (part of `optional_variant_start_pack`, but usable standalone in the *Full Game only, not the Short Game*) — introduces two Group C corporations, **On-Site Refining (OSR)** and **Mining Robotics (MR)**, available whenever RCC/OPC become available:
  - Both start the game **already in play** (not placed via the normal base-purchase flow): randomly draw two asteroid tiles, place OSR's base near Drill Hound's starting hex (with a flat `+10/all-ore-types` counter on that tile's refuel slot) and MR's base near Ice Finder's starting hex (with a "no bonus" counter on its refuel slot). Refueling stations may **never** be placed at either of these two specific starting bases (an explicit exception).
  - Adds extra ships to the depot: **+1 Phase III, +1 Phase V, +3 Phase VI** spaceship (on top of the base game's counts) — matches the per-ship-copy detail in the printed card list (one extra 5/4↔7/3, one extra 7/6↔9/5, three extra 9/7).
  - **Rule change affecting the whole game once these corps are added**: upgrading to a Phase VI ship normally triggers once the *first* Phase V ship is bought — with OSR/MR added, it instead requires **two** Phase V ships bought first, not one. This would need a `G2038::Game` (or a dedicated subclass/module) to change the Phase VI train's `available_on`/phase-change trigger conditionally on this optional rule.
  - Cert limit increases (table matches our existing base-game `CERT_LIMIT` exactly for the "Full Game" column, confirming that hasn't changed): 3p 22→27, 4p 16→20, 5p 13→16, 6p 11→13.
  - Mars Mining gains **+2 claims** to its lifetime `claim_limit` when these corps are in play (corp summary lists MM at 8 total under this variant — cross-check against whatever MM's current `claim_limit:` is in `entities.rb` to get the exact delta right).
  - OSR and MR each have their own non-standard claim-cost schedule (exceptions to the normal escalating-tier rule, §7.43/7.45 in the original rules' numbering): **OSR** costs $80 (1st) / $120 (2nd) per claim, gets a flat **+$10 bonus per delivered claim regardless of ore type** (not gated to one ore like the other corps' bonuses), and claims specifically *bought from an Independent* cost an extra +$20 (of which that +$20 specifically goes to the bank, not the selling independent) — this interacts directly with this session's newly-built "buy a claim from an independent for a flat price" feature (Phase 5b) and would need a per-corp price override there, not just a flat `INDEPENDENT_CLAIM_PRICE` for everyone. **MR** costs a flat **$40 for both the 1st and 2nd claim** placed in a round (no escalation at all).
  - Entities.rb data needed: both corps' full `COMPANIES`/`CORPORATIONS`-style entries (delivery bonus, bases/refuel/claims counts, claim cost schedule, colors, logos, home coordinates derived from the "near Drill Hound"/"near Ice Finder" placement rule above) — none of this exists yet.

- [ ] **13c. Variant Start Packet** (`optional_variant_start_pack`, builds on 13b — always includes the two new corporations too) — a further alternate setup on top of 13b:
  - Replaces the certificates for Private Companies #0 (Planetary Imports), #7 (Tunnel Systems), #8 (Vacuum Associates), #9 (Robot Smelters), #10 (renamed **Space Exploration Co.**, was Space Transportation Co.), and #11 (Asteroid Export Co.) with cheaper variant printings — **critically, #7/#8/#9 (TS/VA/RS) no longer grant a TSI share at all** in this variant (their face value drops accordingly: TS $120→$20, VA $140→$40, RS $160→$60; income unchanged at $5/$10/$15). PI/ST(renamed)/AE keep their TSI/AL share grants and roughly similar values (PI $50→$35, ST $180→$160 income unchanged $20, AE $180→$160 income unchanged $30).
  - Adds **+$300 total starting money**, divided by player count (on top of the normal per-player starting cash) — table in the doc reads oddly due to PDF column extraction but appears to be $700/$525/$420/$350 for 3/4/5/6 players as the *final* per-player total, not just the delta; needs the base rules' own starting-cash-per-player-count numbers to back out the exact "+$300/N" addition cleanly.
  - Adds **+2 Phase I ships** (`3/2`/`5/1`) to the depot, on top of 13b's own additions.
  - **TSI's par price is player-chosen, not fixed** — set by whoever becomes TSI's president as soon as ST (Space Exploration Co.) is purchased, rather than the fixed $100/$67 split used otherwise. TSI is still always a public corporation (never a Growth Corp).
  - **TSI's operate-eligibility changes**: once all 12 Private/Independent companies are sold, TSI will not actually operate until **three more TSI shares** have been bought beyond that point (a different/additional threshold from the normal float mechanic, worth reconciling carefully against the existing pre-float Probe-flying machinery built this session — the doc's own design notes call out that ensuring TSI floats "by having 50% of its shares sold" matters strategically in this variant specifically, implying the threshold is share-count based, similar in spirit to but distinct from the normal `float_percent` check).
  - Once all 12 companies are sold, **all corporations except AL become launchable immediately** — the A/B/C group-partitioning is ignored entirely (this part is already stubbed in: `return if optional_variant_start_pack` in `setup`, before the group-partition logic runs). Growth Corporations still may not be launched during Phase I even so.
  - Uses the **optional "Stock Repurchases by Corporations" rule** as a real (not further-optional) part of this variant — three sub-rule changes: (1) shares in the Growth Corporation's own share box are always bought at the *higher* of that corp's par or current stock price, with the proceeds going to the corp's own treasury, not the bank; (2) a corporation collects dividends only for shares sitting in its own Growth Corp box or the AL trade-in box — never for its own shares sitting in the ordinary stock market; (3) a corporation (including a fully public one) may, using only its own treasury cash and only immediately before/after the route-running step of its own operating turn, buy back one or more of its own shares from the open market at current stock price, moving them into its Growth Corp box. None of this exists in code yet — it's a real, if optional-flavored, mechanic (share buybacks, box-specific dividend eligibility) that doesn't correspond to anything in the base game's dividend/share machinery.
  - Confirmed independently by this same document: Phase III ships (5/4/7/3) are marked "Obsolete: Phase VI" on their own printed cards, and Phase V (7/6/9/5) / Phase VI (9/7) are marked "Permanent" — matching the `rusts_on` fix applied to `5/4`/`7/3` this session (and confirming 6/5/7/6/9/7 correctly staying un-rusted).

---

## Phase 14: Quality-of-Life / Workflow

**Goal:** Convenience features that save repetitive clicking. Not requested for immediate implementation — parked for later.

- [ ] **Repeat a ship's prior-OR flight** — let a player re-run the exact same route (launch hex, every hop, every pickup, and the delivery) a specific ship flew last OR, in one action, instead of re-clicking the whole trip by hand. Needs design: what happens when the board has changed since then (a mine along the old route now claimed/depleted by someone else, a hex explored differently isn't possible since exploration is deterministic, a refueling station added/removed) — probably falls back to a normal manual turn (or stops early) rather than silently improvising a different route. Also needs a decision on where this data comes from (`@route_stats_by_train`/`OperatingInfo` already record some per-route history per turn, but nothing currently persists a full hop-by-hop replayable path across OR boundaries) and how it's surfaced in the UI (a new choice/button per ship, likely in the ship-selector panel next to "Launch").

- [x] **Autorouter (single-ship) — automatically find the best route for one ship**. Requested by the user (2026-08-02). Confirmed not reusing `Engine::AutoRouter` (track/path-based, nothing to attach to for a hex-adjacency game) -- built as a parallel implementation, `G2038::Autorouter` (`lib/engine/game/g_2038/autorouter.rb`): a budget-constrained orienteering DFS over the hex-adjacency graph, branch-and-bound pruned, reusing `Game#trace_revenue`/`#pickup_value`/`#transshipment_value` for the actual revenue math so it can never drift from what a manually-flown route would earn.
  - **Unexplored hexes**: confirmed with the user -- flown over as normal (zero-value) 1 MP flyovers, never explored on the player's behalf.
  - **Performance**: a real bug found live (the search could time out before ever finding even a guaranteed-free transshipment credit, once unexplored hexes became legal flyovers and blew up the raw search space) -- fixed with a fast BFS baseline scan that seeds a guaranteed-reachable answer before the expensive exhaustive search runs, plus a wall-clock deadline (4s default, mirroring `Engine::AutoRouter`'s own timeout philosophy) so a worst-case search still returns the best candidate found rather than hanging.
  - **UI flow**: "Suggest Route" computes and *previews* a route (shown on the map and in the ship's row/button) without touching the log or real game state; a separate "Accept Route" button commits it for real, replaying hop-by-hop through the same `launch_at`/`move_to`/`pick_up` methods a hand-flown route uses. While a suggestion is pending, clicking the hex at its endpoint trims that last leg off (repeatable back to the base), mirroring the standard `RouteSelector`'s click-the-endpoint-to-shorten idiom -- confirmed with the user that mid-route rerouting isn't needed, only end-trimming.
  - **Not done yet**: multi-ship joint optimization (routing all of an entity's unrun ships together in one pass, rather than one at a time) -- the single-ship version was the agreed MVP; revisit if wanted.

---

## Recommended Implementation Order

1. - [x] Phase 1 — Data fixes (foundation for everything) *(PR0/PR1/PR4)*
2. - [x] Phase 3a–3e — OR structure *(PR2/PR3)*
3. - [x] Phase 4a–4b — Basic routing: distance and connectivity validation *(PR4)*
4. - [x] Phase 5 — Mine/claim/base state (needed for routing to earn real revenue) *(PR5)*
5. - [x] Phase 4c remainder, 4d–4j — Full routing: pickup selection, exploration, refueling *(PR5/PR6)*
6. - [ ] Phase 7 — Independent special abilities *(PR7)*
7. - [x] Phase 2 remainder — AE cert timing (2c), auction ordering verification (2e) *(PR6)*
8. - [x] Phase 6 — TSI probe *(PR6)*
9. - [x] Phase 8 — Growth Corp formation *(PR8)*
10. - [x] Phase 9 — Asteroid League formation + independent mergers *(PR9)*
11. - [x] Phase 10 — Private company post-auction abilities *(PR10)*
12. - [x] Phase 11 — Endgame scoring *(PR11)*

---

## Key Architectural Decisions

### A. Mine revenue model
- [x] **Chosen approach:** Store mine state in a `@mine_state` hash on the game object; pickup logic looks up claimed/unclaimed values dynamically. Tile codes display the real unclaimed value for reference, but revenue is never computed from tile codes. *(PR4)*
- [x] **`MINE_DATA` constant** maps each asteroid tile number (2001–2022) to an ordered array of mine hashes (`{ore:, unclaimed:, claimed:}`). `explore_hex!` merges these with `owner: nil, used: false` into `@mine_state` when a tile is placed. *(PR4)*
- [x] **Bases and mines are mutually exclusive on a hex.** Placing a base flips the tile to 2023 (no mines). `@mine_state` only ever tracks un-based explored hexes. *(PR5)*

### B. Exploration undo policy
- [x] **Chosen approach:** Stay fully faithful to the physical game — asteroid tiles are revealed immediately when explored, mid-route. Undo is **not** blocked. Instead, confirmed with the user: log without disclosing content — any undo that reaches back far enough to un-happen a specific exploration gets a permanent log entry naming the player and the hex (not what was found there), so other players can police suspected peeking without the fix itself broadening the information leak. See Phase 4i for the implementation. *(decision PR4; implementation post-Phase-11)*

  **How the engine works (important context):**
  - When an undo arrives, `Game::Base#process_action` immediately calls `return clone(@raw_actions)` — the game is completely rebuilt from scratch. No game-specific hooks (`preprocess_action`, etc.) fire for undo actions.
  - `@log` is rebuilt from scratch on every clone/undo.
  - `Action::Log` (which extends `Action::Message`) is **never** filtered out by `filtered_actions` — messages survive all undos and are replayed in the correct chronological position.
  - `initialize_actions` is overridable in a game subclass without touching engine code.

  **Implementation approach — override `initialize_actions` in `G2038::Game`:**
  - After calling `super` (which replays all actions and builds `@log`), scan `@raw_all_actions` for any `{ 'type' => 'undo' }` entries
  - For each undo found, look up the player via `action['user']`, identify the action ID range that was undone via `action['action_id']`
  - Append a styled log entry: `"⚠ [UNDO] PlayerName undid action(s) #Y–#Z"`
  - These entries appear at the bottom of the log (not inline), but are permanent — they survive all further undos because they are regenerated fresh on every rebuild

  **Implementation tasks:**
  - [ ] Override `initialize_actions` in `G2038::Game`; after `super`, scan `@raw_all_actions` for undo entries and build log entries *(PR6)*
  - [ ] Only emit undo log entries during Operating Rounds (check round type at the action's point in history, or always log and rely on the action ID range being self-explanatory) *(PR6)*
  - [ ] Format the log entry to include: player name, action ID range undone, and round/turn context if determinable *(PR6)*
  - [ ] Undo announcements must persist even if the player subsequently undoes further actions (guaranteed by the rebuild approach above) *(PR6)*
  - [ ] Investigate whether `self.filtered_actions` override could instead inject a synthetic `message`-type hash at the undo's chronological position, which would make the log entry appear in-order rather than at the bottom — this is a stretch goal if in-order logging matters *(PR6)*

### C. Spaceship distance model and pickup selection
- [x] **Chosen approach (revised PR4): choose-per-hex tracing.** Spaceships stay in TRAINS for purchase/obsolescence tracking. `G2038::Step::Route` inherits `Engine::Step::Base` and traces routes one hex per `choose` action — no node/chain path-walking, no engine changes. `Engine::Route` is used only as a passive container (seeded `hexes:`/`revenue:`) handed to the dividend step. *(PR4)*
- [x] **Pickup selection UX (revised PR4): pickup is a per-hex-entry choice, per the rules.** While a ship sits on a mine hex, `choices` offers a "Pick up X ore" entry per available mine (hold space permitting). Loads cannot be jettisoned once aboard — picking up early can block a better mine found later in the run, which is a real decision the player must make. The Used Mine marker is placed at pickup time; cancelling a route returns its loads and un-marks their mines. *(PR4)*
  - [x] `pickup_value(entity, hex_id, mine_idx)` — returns claimed or unclaimed value *(PR4)*
  - [x] `trace_revenue(entity, train, trace, cargo)` — cargo pays out only when the run ends at a base or transshipment point; ending at a transshipment point with **empty** holds earns its printed value; undelivered loads are lost (logged) *(PR4)*
- [x] **`deliverable_destination?(hex)`** — returns true if hex is a transshipment point or has any placed base token; used by `trace_revenue` to gate delivery *(PR4)*

### D. Unexplored hex representation
- [x] **Chosen approach:** Pre-generate all tile assignments at `setup` using a seeded shuffle of the full tile pool. Store in `@hex_assignments` (hex_id → tile_name, server-side only, never sent to clients). When a ship's `Choose` move enters an unexplored hex, the server looks up the pre-assignment, places the tile, and reveals it to all players simultaneously. *(implemented PR4)*

  **Why pre-generate rather than draw from a live bag:**
  - There are exactly **106 blue hexes and 106 mine tiles** — a perfect 1:1 match, so a complete pre-assignment is always possible.
  - `@hex_assignments` is derived deterministically from `@seed` in `setup`, so it is rebuilt identically on every clone/undo without being stored in `@raw_actions`.
  - Undoing an `ExploreHex` action naturally un-reveals the tile — the game rebuilds from `@raw_actions`, which no longer contains the explore action, so the hex reverts to blue. The pre-assignment in `@hex_assignments` is still intact for the next exploration.
  - `Action::ExploreHex` only needs a hex_id — no tile name, no bag state.
  - No "bag" shrinks over time; no bag state to maintain or serialize.

  **Implementation:**
  - [x] `assign_exploration_tiles` in `setup`: sorted blue hex ids zipped with the TILES-count tile pool shuffled via the engine's deterministic LCG `rand` (identical in Opal/MRI; `Random.new` is not cross-platform-safe) *(PR4)*
  - [x] `explore_hex!(hex_id, entity)`: looks up `@hex_assignments[hex_id]`, lays an unplaced tile copy, updates `@mine_state`, awards the $10 exploration bonus, logs the event. Fired from `process_choose` when a ship enters an unexplored blue hex (no separate `ExploreHex` action needed — the `Choose` move action carries the hex id) *(PR4)*
  - [ ] Verify that `hex.lay` during an OR (outside the normal `LayTile` step) is fully reflected in serialized game state across reload *(PR4 testing)*
  - [x] **Tile counts confirmed correct:** 100 blue hexes, 106 tiles — the 6 surplus tiles are deliberately unassigned spares for second-draw special powers *(PR4)*
  - [ ] **Second-draw powers (IF/DH forced redraw, Lucky's two-tile choice):** when a power grants an extra tile draw, pick a random *unexplored* hex, temporarily take its pre-assigned tile as the second draw, and put whichever tile the player doesn't use back into that hex's assignment. No one knows which unexplored hex was borrowed from, so the brief reveal-and-return leaks nothing. Requires the extra-draw picker to use the deterministic `rand` so replays match. *(PR7)*

### E. Infrastructure placement model
- [x] **Bases → Token system.** Tokens occupy a city slot (one base per hex), have placement costs, belong to a corporation, and establish route origins. The 13 pre-printed starting bases map to home tokens at each entity's starting coordinates. *(PR5)*
  - [x] **Independent companies have exactly one base — their home base.** One free home token, no additional tokens. `deliverable_destination?` returns true for any hex with a placed token, so independent bases correctly qualify as delivery destinations for any ship. *(PR4)*
- [x] **Refueling Stations → Modifier on a base.** Will be stored as a `@refueling_stations` hash (`hex_id → corporation`). When any ship owned by the station's corporation enters that hex, remaining MP is increased by 3, capped at the ship's original MP: `new_remaining = [remaining + 3, original_mp].min`. One refueling station per base; usable once per ship per OR. *(PR5)*
- [x] **Claims → Revenue modifier only.** Stored in `@mine_state` as `mine[:owner]`. A claim marks a mine as owned by a specific company; that company's ships earn `mine[:claimed]`, all others earn `mine[:unclaimed]`. Claims have no effect on routing or movement. *(PR4)*
  - [x] **Independent companies each have 2 claims** (not tokens — handled as custom claim state in `@mine_state`). *(PR5)*
- [x] **Bases and claims are mutually exclusive** — enforced by rule (bases require a tile without claims; the tile flip to 2023 removes mines). No code-level guards needed. *(PR5)*

---

## Reference: Key Files in Other Games

| Mechanism | Game | File |
|---|---|---|
| BY-share privates (shares ability) | 1835 | `g_1835/entities.rb` |
| Minor-to-major conversion (Prussian) | 1835 | `g_1835/step/form_prussian.rb`, `g_1835/game.rb` |
| Sequential merger step | 1835 | `g_1835/step/merge_to_prussian.rb` |
| Distance-based train routing | 18Ireland | `g_18_ireland/game.rb` (`route_distance`, `check_distance`) |
| Minor company operating model | 1835, 1861 | `g_1835/minor.rb`, `g_1861/game.rb` |
